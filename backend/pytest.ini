[pytest]
asyncio_mode = auto
asyncio_default_fixture_loop_scope = function
testpaths = tests
pythonpath = .
filterwarnings =
    ignore::DeprecationWarning
markers =
    integration: tests requiring MySQL (set RUN_MYSQL_TESTS=1)
